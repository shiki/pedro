
import db from '../services/db'

/**
 * Used for creating an access token based off of parameters from POST /token. This will create a user if no user exists with the given `anonUUID`.
 */
export function create({ anonUUID, grantType, clientId }) {

}
